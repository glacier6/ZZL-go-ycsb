这个测试记录是在load和run之后，分别多等待一段时间，以让LSM-TREE结构恢复到健康状态！
等待时间为非固定时间！

tv的表示测的是Vlog回收，配置如下
recordcount=30000000 （或50000000）
operationcount=30000000（或50000000）
workload=core

readallfields=true

readproportion=0.5
updateproportion=0.5
scanproportion=0
insertproportion=0

fieldcount=1
fieldlength=2048 （配置为1024为分裂阈值） 	# 注意修改分裂阈值，在NOTE:2026062200
fieldlengthdistribution=uniform

requestdistribution=zipfian

运行时
THREAD_COUNTS=(8)



统计当前这个文件同级目录下的各个测试日志，目录名就是load阶段的数据量级，而run阶段都是1000w次，然后每个测试文件都会很长，但是你不必要全部看完。
1000w表示是在小机器的测试结果，这方面不用比较。先只比较t开头的文件夹
对比的对象是文件名分别包含heatLSM和Normal的测试结果
对于如t1000w等t*文件夹需要你统计的如下（t*的不需要看GC）：
1.各个Round1代表的是load+run阶段，Round2,3的则是直接复制Round1的load，我需要你统计出各线程并发数（文件名的Tx）下的各个Round1的Load的QPS（注意是在load阶段中，等待 LSM-tree compaction 趋于健康之前输出的那个Run finished***）。
2.然后再计算出来三轮Run的平均执行时间以及其延迟分布对比
3.然后再计算出三轮Run的写放大均值，并且对比
对于如tv3000w等tv*文件夹，这里主要是要看GC的回收效率，每次的load和run的数量级这里是相等的，需要你统计的如下：
1.比较3轮GC造成了多大的额外写放大
2.比较3轮GC回收了多少空间
然后把比较结果写入/home/hanjiang/DB-CODE/ZZL-go-ycsb/zzl_test_log4/result.md，注意展示一定的计算过程，我要看你是否计算正确
