现在对比试验候选项
(1) BoltDB 纯 Go 编写的简单 KV 存储（采用 B+ 树结构）。
(2) TiKV (Rust语言),这个是写go-ycsb官方的数据库,内部嵌套rocksDB,而且是分布式的,不是单机的,所以也不太公平,不行!!!
(3) etcd也不行,不是LSM-tree的
NOTE:所以现在的对比试验基本只能和badger自身做对比了.....

RocksDB(NOTE:下面这个6.11.4版本貌似还是太高了,估计还得降低版本,超过1000条的run就会报错!!暂时先不管了)
报SIGSEGV: segmentation violation //// PC=0x76bcfa8b44d9 m=12 sigcode=1 addr=0x76bc7877fff8 //// signal arrived during cgo execution 错误
即内存越界,大概率还是因为版本不兼容问题!!!!!!!!!!!!!!!!!!!!
1.先安装需要的编译工具
  sudo apt-get update
  sudo apt-get install -y build-essential cmake git
  V10:sudo apt-get install -y libgflags-dev libsnappy-dev zlib1g-dev libbz2-dev liblz4-dev libzstd-dev
  V6:sudo apt-get install -y libsnappy-dev liblz4-dev libzstd-dev libbz2-dev
  PS:注意可能下载失败,注意换源,这几个是RorcksDB的压缩算法
2.git clone项目后之后先切换到v6.11.4用于测试(25年3月份的10.0.1版本,但是这一个版本有兼容性问题)
  git checkout v6.11.4
3.进入RocksDB根目录编译出程序
  v6老版本:DISABLE_WARNING_AS_ERROR=1 PORTABLE=1 EXTRA_CXXFLAGS="-include cstdint" make shared_lib -j$(nproc)
  v10新版本:DISABLE_WARNING_AS_ERROR=1 PORTABLE=1 make shared_lib -j$(nproc)
  PS:如果编译失败需要清理,则执行make clear
4.安装到系统目录
  sudo make install-shared
5.刷新动态链接器
  sudo ldconfig
6.切换到YCSB内,执行打包
  CGO_CFLAGS="-I/usr/local/include" CGO_LDFLAGS="-L/usr/local/lib -lrocksdb -lstdc++ -lm -lz -lbz2 -lsnappy -llz4 -lzstd" go build -tags rocksdb -o ./bin/go-ycsb ./cmd/go-ycsb